# 项目工作进展总结

> 最后更新：2026-09-05

本文档记录「计算思维诊断与个性化干预系统」的当前状态，便于下次打开快速接续工作。

---

## 一、项目概述

**定位**：双通道、三层因果可解释 AI 的教育诊断与个性化干预系统，服务于「可解释 AI 驱动计算思维个性化干预」这篇论文 + 下学期（高二）真实教学实验。

**核心链路**：量表/代码输入 → XGBoost 预测 → 三层诊断（反事实 / SHAP×因果 / ITE）→ 四分类干预建议 → 个性化思维训练题单 → 纸质分发。

---

## 二、系统当前能力（已完成）

### 1. 量表数据收集与预处理
- [make_scale_template.py](make_scale_template.py)：生成带数据校验的 Excel 模板（A1–A15 题 + 已有成绩）
- [preprocess_scale_data.py](preprocess_scale_data.py)：原始 15 题（A–E 字母或 1–5 数字）→ 五维度得分 + 归一化成绩
- [utils/scale_data.py](utils/scale_data.py)：维度映射（分解 A1-3、抽象 A4-6、建模 A7-8、算法设计 A9-10、评估 A11-15）
- 量表来源：张屹「中小学生计算思维量表」（15 题五维度，与系统映射完全一致）

### 2. 个性化干预出题（跨周去重 + 一人一单）
- [data/question_bank.json](data/question_bank.json)：**150 道**不插电母题（五维各 30 道），难度按 FADE-CTP 结构属性分 1–3 星，每题含 `train_target` / `source`
- 科学依据：[paper/训练题设计依据.md](paper/训练题设计依据.md)——Bebras 题型 + FADE-CTP 设计框架 + 不插电训练元分析
- **跨周去重**：后端持久化 `data/intervention_history.json`，抽题自动跳过已发过的题，题库不足时明确告警；「清空出题历史」可重开一轮
- **一人一单**：批量诊断后按每名学生薄弱维度（每周最多 2 维，可调）自动组个人题单 + 分发清单 CSV（上传表格可加「姓名」「班级」列）
- 保留「手动按维度出题」作备用；出题不再依赖大模型（变式已由内置题库替代）

### 3. 三层诊断（含方法学修正）
- Layer 1 反事实分析、Layer 2 SHAP×因果双维度、Layer 3 ITE
- **个体化调制**：引入「提升空间」`headroom = max(0, 5 − score)`，将群体 CE 调制成个体化效应 `CE_ind = CE × headroom`，消除「满分维度仍建议干预」的生态谬误
- CE 单位统一为「每 1 分」的边际效应（OLS 与分组比较两条路径一致）
- 散点图与表格分类同步个体化

### 4. 性能优化
- SHAP explainer 与自适应阈值模块级缓存（二次诊断快约 3 倍）

### 5. API Key 管理
- 侧边栏自定义 DeepSeek API Key（每位教师用自己的 key，不消耗部署者额度）
- 已移除 st.secrets 自动同步；无 key 时润色回退模板、变式回退母题

### 6. 桌面版（Windows exe）
- [launcher.py](launcher.py) + [CT_diagnostic.spec](CT_diagnostic.spec)（PyInstaller 打包）
- 产物：`dist/CT_Diagnostic/CT_Diagnostic.exe`（约 850MB，双击即用，数据本地）
- [启动系统.bat](启动系统.bat)：一键启动脚本（需先装 Python 的过渡方案）
- [桌面版使用说明.md](桌面版使用说明.md)

---

## 三、运行方式

| 方式 | 命令 | 适用 |
|------|------|------|
| 桌面 exe | 双击 `dist/CT_Diagnostic/CT_Diagnostic.exe` | **正式实验**（本地跑，无云端限流） |
| 源码 | `streamlit run app.py` | 开发调试 |
| 云端 | Streamlit Cloud（连接 GitHub） | 演示/备用（免费版会限流） |

重新打包：`pyinstaller CT_diagnostic.spec --noconfirm`（先 `taskkill //F //IM CT_Diagnostic.exe` 杀进程）

---

## 四、实验准备状态

| 事项 | 状态 |
|------|------|
| 实验设计 | ✅ 已确认（见下） |
| 量表 | ✅ 张屹量表，15 题五维度，已对齐 |
| 前测数据收集 | ⏳ 下周发量表 |
| 模型训练（真实数据） | ⏳ 前测数据回来后 |
| 干预材料 | ✅ 出题系统 + 50 道母题（可扩充） |
| 对照组材料 | ❌ 未定（需等时长的常规练习） |
| 伦理审批（IRB） | ❌ 未开始 |
| 论文写作 | 📝 大纲 + 文献综述已有 |

### 已确认的实验设计
- 对象：高二 6 个班，约 300 学生
- 分组：**整班分组**（校方不同意随机，属准实验设计）
- outcome：**已有成绩**（前测时能拿到「五维度 → 成绩」成对数据，训练真实模型）
- 流程：前测 → 诊断 → 实验组按报告干预（对照组不干预）→ 两个月后后测
- 训练原则：用**全部 300 人**前测数据训练（不分实验/对照）

---

## 五、关键决策记录

1. **出题粒度**：按薄弱维度分组（非一人一题单），配分发清单解决「老师不知道发给谁」
2. **题目来源**：混合（预置题库打底 + DeepSeek 生成变式扩充）
3. **输出格式**：Word（.docx），可打印
4. **个体化诊断**：提升空间调制 CE（有公式依据，非拍脑袋阈值）
5. **API Key**：教师侧边栏自输入，不用部署者 key
6. **部署**：桌面 exe 为主，云端 Streamlit 演示备用

---

## 六、待办 / 下一步

### 紧急（实验前）
- [ ] 前测数据回来后的**一键脚本**：训练真实模型 + 基线平衡检验（实验班 vs 对照班无显著差异）+ 班级配对
- [ ] 对照组材料标准化（等时长、等难度的常规练习）

### 重要
- [ ] 题库扩充（目前每维度 10 道，两个月 6-8 次课若不开变式会重复）
- [ ] 干预操作手册（教师版：每周怎么发题、记录完成情况）
- [ ] 伦理审批（IRB 申请表 + 知情同意书）

### 论文相关
- [ ] 用 scientific-agent-skills 推进论文（见 [paper_outline.md](paper/paper_outline.md) 的「参考技能映射」）
- [ ] 基线对比预跑（`python benchmark.py`）
- [ ] Introduction / Methodology 形式化
- [ ] 画 Fig.1-4

### 已知待改进
- [ ] CE 单位统一后的阈值验证（统一后 ce_threshold 中位数变小，需确认分类敏感性）
- [ ] Streamlit 云端免费版限流 → 正式实验必须用桌面 exe

---

## 七、重要文件速查

| 文件 | 作用 |
|------|------|
| [app.py](app.py) | Streamlit 主入口 |
| [completeV6_patched.py](completeV6_patched.py) | 三层诊断核心引擎 |
| [source_code_evaluator_patched.py](source_code_evaluator_patched.py) | 代码 AST 静态评估 |
| [utils/intervention.py](utils/intervention.py) | 干预出题模块 |
| [utils/scale_data.py](utils/scale_data.py) | 量表维度映射 |
| [data/question_bank.json](data/question_bank.json) | 题库（50 道母题） |
| [paper/paper_outline.md](paper/paper_outline.md) | 论文大纲 |
| [paper/experimental_design.md](paper/experimental_design.md) | 实验方案 |
| [paper/literature_review.md](paper/literature_review.md) | 文献综述 |
| [研究报告_可解释AI计算思维个性化干预.md](研究报告_可解释AI计算思维个性化干预.md) | 技术研究报告 |
| [CT_diagnostic.spec](CT_diagnostic.spec) | PyInstaller 打包配置 |
