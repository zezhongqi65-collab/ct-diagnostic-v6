# 可解释人工智能面向计算思维培养的信息技术个性化干预研究

## 技术研究报告

---

**摘要**：本报告介绍一套基于可解释人工智能（Explainable AI, XAI）的计算思维诊断与个性化干预系统。该系统融合机器学习预测、模型归因解释、因果推断三大技术路径，构建了"反事实分析→双维度诊断→个体处理效应估计"的三层递进诊断架构，并创新性地提出了"模板填充—大模型润色—程序校验"的三层保真报告生成机制。系统支持量表数据与Python源代码双通道输入，可自动评估学生计算思维的五个核心维度，生成面向一线教师的四分类干预建议。

**关键词**：可解释人工智能；计算思维；因果推断；SHAP；个性化干预；AST静态分析

---

## 一、研究背景与问题

### 1.1 计算思维教育的核心挑战

计算思维（Computational Thinking, CT）被公认为21世纪学生的核心素养之一，其内涵包括将复杂问题抽象化、分解为可处理的子问题、设计算法化的解决方案、建立数学模型以及评估方案的有效性。在信息技术课程中，教师面临的核心困境是：**如何准确诊断每个学生在计算思维各维度上的薄弱点，并有针对性地给予干预？**

传统的教育评估依赖量表得分和教师经验判断，存在三个局限：

1. **单一总分掩盖多维结构**：一个80分的学生，可能"算法设计"很强但"问题分解"薄弱，而另一个同样80分的学生薄弱点完全不同。总分无法区分这些差异。
2. **相关性不等于因果性**：即使我们能识别出某维度得分低与成绩差之间的相关性，也无法判断"提升该维度"是否真的能带来成绩改善。这需要因果推断的介入。
3. **诊断结果难以转化为教学行动**：纯数据报告对一线教师不够友好，需要将分析结果转化为可理解、可操作的干预建议。

### 1.2 可解释AI的介入点

传统机器学习模型通常是"黑箱"——输入数据、输出预测，但中间过程不可见。在教育场景中，黑箱模型无法满足需求：教师需要知道**为什么**给出某个诊断结论，以及**如何**据此采取行动。

本系统引入三类可解释AI技术：

| 技术 | 核心问题 | 对应诊断层级 |
|------|----------|-------------|
| **SHAP值分析** | 哪些维度对模型预测贡献最大？ | Layer 2（模型归因） |
| **因果推断（后门调整）** | 单独提升某维度，是否真能提高成绩？ | Layer 2（因果效应） |
| **反事实模拟** | 如果某维度提升至满分，预测会如何变化？ | Layer 1（假设推演） |

三种技术从不同角度回答问题，形成互补：SHAP告诉你"模型认为什么重要"，因果效应告诉你"干预是否真的有效"，反事实分析则提供一个直观的"假设推演"场景。

---

## 二、计算思维五维度评估模型

本系统采用国际计算思维研究中的经典五维度框架。下表从教育含义和技术测量两个层面进行说明：

| 维度 | 教育含义 | 量表测量方式 | 代码自动评估方式 |
|------|----------|-------------|-----------------|
| **抽象** | 识别模式、提炼共性、忽略无关细节的能力 | 3道5点李克特自评题 | AST检测：函数/类定义数量、继承层次、抽象方法、装饰器使用、命名常量vs魔法数字比 |
| **分解** | 将大问题拆解为可管理的小问题的能力 | 3道5点李克特自评题 | AST检测：函数密度（每行代码对应函数数）、函数长度分布、类方法拆分粒度 |
| **算法设计** | 设计有序步骤、选择合适数据结构解决问题的能力 | 2道5点李克特自评题 | AST检测：Set/Hash使用（O(1)查找意识）、推导式使用、内置高效函数调用、嵌套深度、递归应用 |
| **建模** | 将现实世界概念转化为数学/符号化表达的能力 | 2道5点李克特自评题 | AST检测：领域类建模数量、dataclass使用、枚举类、类型注解覆盖率、NamedTuple |
| **评估** | 检验方案正确性、发现并修正错误的能力 | 5道5点李克特自评题 | AST检测：测试函数、assert断言、try/except异常处理、输入验证、docstring覆盖率、logging使用 |

**量表总题数**：15题（A1-A15），每题1-5分，按维度分组计算平均分，再经加权公式综合评估。

**代码评估原理**：通过AST（Abstract Syntax Tree，抽象语法树）静态分析技术，在不运行代码的情况下解析Python程序的语法结构树，从中提取上述五维度的行为指标。每个维度的得分由若干子特征加权合成（详见第七章）。

---

## 三、系统总体架构

系统采用"双通道输入 → 统一诊断管道 → 三层保真报告"的架构设计。

### 3.1 双通道输入设计

```
          ┌─────────────────┐
          │   用户输入层     │
          ├────────┬────────┤
          │ 通道A  │ 通道B  │
          │ 量表   │ 源代码 │
          │ CSV/   │ .py    │
          │ Excel  │ ZIP    │
          └───┬────┴───┬────┘
              │        │
              ▼        ▼
     ┌────────┐  ┌──────────┐
     │五维度  │  │AST静态分析│
     │得分    │  │→ 五维度  │
     │直接输入│  │  得分    │
     └───┬────┘  └─────┬────┘
         │              │
         └──────┬───────┘
                ▼
     ┌──────────────────┐
     │  XGBoost预测模型  │
     │  (作业质量→100分) │
     └────────┬─────────┘
              │
              ▼
     ┌──────────────────┐
     │ 三层因果诊断管道  │
     ├──────────────────┤
     │ Layer 1: 反事实   │
     │ Layer 2: SHAP×因果│
     │ Layer 3: ITE估计  │
     └────────┬─────────┘
              │
              ▼
     ┌──────────────────┐
     │ 三层保真报告生成  │
     ├──────────────────┤
     │ 模板→润色→校验   │
     └──────────────────┘
```

### 3.2 因果图（DAG）

系统内置了基于教育理论的计算思维因果关系图（DAG, Directed Acyclic Graph，有向无环图）。DAG是一种用于表达变量间因果依赖关系的图形工具：箭头从"原因"指向"结果"。

本系统的因果假设如下：

```
抽象 ──→ 分解
抽象 ──→ 算法设计
算法设计 ──→ 建模
算法设计 ──→ 评估
```

这一结构反映了教育理论中的认知层次：学生首先需要**抽象**能力来识别问题模式，这影响其**分解**问题和**设计算法**的能力；而算法设计能力又进一步影响**建模**和**评估**的水平。在因果效应估计中，该DAG用于确定哪些变量需要作为"混淆因子"进行控制。

---

## 四、核心技术方法

### 4.1 预测模型：XGBoost（极限梯度提升）

**技术解释**：XGBoost（Extreme Gradient Boosting）是一种基于"集成学习"思想的机器学习算法。它的核心思路可以类比为"专家会诊"：与其依赖一个专家（单棵决策树）的判断，不如让多位专家依次发表意见，每位专家重点纠正前一位的错误。经过多轮迭代，最终的综合判断比任何单个专家都更准确。

**为什么选择XGBoost？**

1. **表格数据表现优异**：在教育评估这类结构化数据（几十到几百行、若干列得分数据）上，XGBoost通常优于深度学习模型
2. **天然支持SHAP解释**：XGBoost的树结构使其可以与SHAP库无缝对接，实现精确的模型解释
3. **防止过拟合**：内置L1/L2正则化（通过对模型复杂度施加"惩罚"来避免模型死记硬背训练数据），在200条样本的小数据集上不易"过拟合"（overfitting，即模型只记住了训练数据却无法泛化到新数据）

**模型配置**：

| 参数 | 设定值 | 含义 |
|------|--------|------|
| n_estimators | 100 | 进行100轮"专家会诊"，每轮添加一棵树 |
| max_depth | 3 | 每棵决策树最多3层，控制模型复杂度 |
| learning_rate | 0.05 | 每轮纠正的步长较小，保证稳定收敛 |
| reg_alpha | 0.1 | L1正则化强度，防止过拟合 |
| subsample | 0.8 | 每轮随机使用80%的样本，增加多样性 |

**输入与输出**：
- 输入：学生的五个维度得分（抽象、分解、算法设计、建模、评估）
- 输出：预测作业质量分数（0-100分），模型拟合精度以RMSE（Root Mean Square Error，均方根误差）衡量，数值越小表示预测越准

### 4.2 SHAP值：模型的可解释性分析

**技术解释**：SHAP（SHapley Additive exPlanations，沙普利可加性解释）是一种基于博弈论中Shapley值的模型解释方法。其核心思想可以理解为"公平分蛋糕"：一个预测结果（如"预测成绩75分"）可以被合理地"分配"给各个输入变量，每个变量分得的份额就是它的SHAP值。

**数学直觉**：想象五个维度是五个玩家，共同完成一道题目（得到75分的预测）。SHAP值的计算方式是：轮流考虑每个玩家加入后带来的改善，对所有可能的加入顺序取平均。如果无论何时"抽象"维度的加入都能让预测大幅提升，那么"抽象"就获得了较大的SHAP值。

**关键属性**：
- **符号**：正SHAP值表示该维度将预测值"拉高"，负值表示"拉低"
- **绝对值**：|SHAP值|的大小反映了该维度对模型预测的重要性
- **可加性**：所有维度的SHAP值之和 + 基准值 = 最终预测值（这一性质确保了解释的完整性）

**自适应阈值**：系统不采用固定阈值（如"SHAP > 0.5就算重要"），而是基于训练集所有样本SHAP值的50%分位数自动计算阈值。这意味着阈值会随数据分布自适应调整，而非人工设定。

### 4.3 因果推断：后门调整（Backdoor Adjustment）

**为什么需要因果推断？**

相关不等于因果，这在教育场景中至关重要。举一个通俗的例子：

> 冰淇淋销量与溺水死亡人数高度**相关**，但这不意味着禁售冰淇淋就能减少溺水。真正的**原因**是夏季高温同时导致了冰淇淋销量和游泳人数的增加。这里的"温度"就是一个**混淆因子**（Confounder）。

同理，在计算思维评估中，"分解维度得分高"与"成绩好"存在相关，但这种相关性可能被第三个因素（如"抽象能力"）所共同驱动。如果不控制混淆因子，我们可能错误地建议教师对"分解"维度进行干预，而实际上真正需要干预的是"抽象"维度。

**后门调整的技术原理**：

后门调整（Backdoor Adjustment）是一种基于因果图（DAG）的统计方法，其操作步骤为：

1. 根据因果图识别每个维度（处理变量）的父节点（指向它的箭头来源），将这些父节点作为混淆因子
2. 在统计模型中"控制"这些混淆因子，即在保持混淆因子不变的情况下，测量处理变量与结果之间的净效应
3. 得到的ATE（Average Treatment Effect，平均处理效应）表示：在其他条件相同的情况下，将某一维度提升 **1 分**，成绩预期提高多少分（即"每 1 分的边际效应"）。系统将 OLS 回归系数与分组比较两条路径的单位统一为"每 1 分"，以便后续做个体化调制

**p值衰减策略**：传统因果推断中，若p值（显著性检验值）大于0.05，通常认为结果"不显著"而直接归零。本系统采用更保守的**衰减策略**：p > 0.05时，因果效应估计值乘以0.3的衰减系数，而非直接归零。这一设计的理由是：在小样本教育场景中，p值不显著可能仅仅因为样本量不足，而非真的没有效应，完全归零会丢弃有价值的信息。

**三层回退机制**：

```
优先方案: statsmodels.OLS 线性回归（控制父节点混淆因子，回归系数即每 1 分边际效应）
    ↓ 失败/不可用
回退方案: 分组比较（在混淆因子各水平内，高分组vs低分组均值差 ÷ 得分跨度，归一化到每 1 分）
    ↓ 仍失败
最终方案: 简单分组比较（高分组整体 vs 低分组整体，同样归一化到每 1 分）
```

### 4.4 反事实分析（Counterfactual Analysis）

**技术解释**：反事实分析（Counterfactual Estimation）回答的是一个"如果……会怎样"的假设性问题。在系统Layer 1中，它具体表现为：**"如果该学生的某个维度得满分（5分），而其他维度保持不变，模型预测的成绩会变化多少？"**

**操作方式**：对每个维度，依次将当前值替换为1、2、3、4、5分，固定其他维度不变，通过已训练好的XGBoost模型重新预测，观察预测值的变化轨迹。

**重要声明**：反事实分析是**模型层面的模拟**，不是真实的因果效应估计。它仅展示"模型认为该维度变化会产生什么影响"。由于XGBoost模型可能存在偏差，反事实分析的结果需要与Layer 2的因果效应结果相互印证。这一区别在报告中已通过显著的免责声明标注。

### 4.5 双维度诊断矩阵（SHAP × 因果效应）—— Layer 2

这是系统的核心诊断引擎。它将SHAP重要性（横轴）与因果效应（纵轴）交叉，形成一个2×2四象限分类矩阵：

```
        因果效应（高）
             │
     💡      │      ✅
  潜在有效   │   优先干预
  (模型不识  │  (模型认为重要
   别但干预  │   且干预真实有效)
   可能有效) │
             │
  ──────────┼──────────→ SHAP重要性（高）
             │
     ➖      │      ⚠️
  无需关注   │   仅观察
  (维度正常) │  (模型认为重要但
             │   单独干预效果有限，
             │   需排查根因维度)
```

**个体化调制（消除生态谬误的关键设计）**：上述因果效应 CE 是**群体层面**的平均处理效应，对所有学生一视同仁，无法回答"这个学生当前该不该干预"。为此，系统引入**提升空间**（5 − 当前得分）将群体效应调制为**个体化因果效应**：

> CE^ind = CE × max(0, 5 − 得分)

- **满分维度**：提升空间为 0，个体化效应为 0，自然落入"无需关注"
- **低分维度**：提升空间大，个体化效应大，才可能进入"优先干预"
- **接近满分**（提升空间不足半格，即得分 ≥ 4.5）：直接判定"无需关注"

这一设计消除了"用群体统计量推断个体"的**生态谬误**，使诊断真正个体化。下文四象限分类中的"因果效应"均为个体化后的 CE^ind。

**四类干预建议**：

| 分类 | 条件 | 含义 | 教学建议 |
|------|------|------|----------|
| ✅ **优先干预** | SHAP高 + 个体化效应高 | 该维度既对模型预测重要，个体又确有提升空间 | 立即安排专项训练 |
| ⚠️ **仅观察** | SHAP高 + 个体化效应低 | 模型认为重要，但该个体单独干预效果有限 | 根据DAG追溯根因维度（上游维度），优先训练根因 |
| 💡 **潜在有效** | SHAP低 + 个体化效应高 | 模型未充分识别该维度作用，但该个体干预可能有效 | 作为二级备选干预目标 |
| ➖ **无需关注** | SHAP低 + 个体化效应低，或接近满分 | 当前维度表现正常，或已无提升空间 | 维持现有教学方式 |

**根因追溯**：当某维度被标记为"⚠️ 仅观察"时，系统利用因果图（DAG）自动向上追踪其父节点（上游影响因素）。例如，若"算法设计"被标记为⚠️，系统会检查其父节点"抽象"的得分——如果"抽象"得分也低于3分，则推测"算法设计"的问题可能是"抽象"能力不足导致的，建议先干预"抽象"这一根因维度。

### 4.6 ITE个体处理效应估计（Layer 3）

**技术解释**：ITE（Individual Treatment Effect，个体处理效应）回答的是：**"对某个特定学生进行干预，他能从干预中获得多大收益？"** 与Layer 2基于"提升空间"的启发式个体化（将群体效应按提升空间缩放）不同，ITE通过X-Learner/T-Learner等方法直接从数据学习个体差异，是更严格的个体处理效应估计。

**本系统实现了两种估计算法**：

**T-Learner（双模型学习器）**：
1. 用对照组的样本训练模型M₀，学习"不干预时的自然成长规律"
2. 用实验组的样本训练模型M₁，学习"受干预后的成长规律"
3. 对于每个学生，ITE = M₁预测值 - M₀预测值（即干预带来的额外收益）

**X-Learner（交叉学习器）**：
在T-Learner的基础上增加了一个"效应模型"阶段：
1. 用M₀预测实验组学生"如果不干预会怎样"，计算真实与预测的偏差D₁
2. 用M₁预测对照组学生"如果干预了会怎样"，计算预测与真实的偏差D₀
3. 训练一个专门预测"D值"（即干预效应）的模型τ（tau），最终ITE = τ(学生特征)

X-Learner通常比T-Learner更精确，因为它将"干预效应"作为专门的学习目标，而非两组预测的简单差值。

**使用前提**：Layer 3需要前测/后测实验数据（学生接受干预前后的得分对比），属于准实验设计范畴。系统内置了实验设计模板，引导教师完成"前测→随机分组→干预→后测"的标准流程。

### 4.7 AST源代码静态分析

**技术解释**：AST（Abstract Syntax Tree，抽象语法树）是编译器解析源代码后生成的树状结构表示。每一行Python代码在AST中都有对应的节点：`def`关键字生成`FunctionDef`节点，`class`生成`ClassDef`节点，`try`生成`Try`节点，等等。

**工作流程**：

```
源代码文件(.py)
    │
    ▼
ast.parse()  [Python内置解析器]
    │
    ▼
抽象语法树 (AST)
    │
    ▼
_MetricsVisitor  [自定义节点访问器]
  - 遍历整棵语法树
  - 收集52类指标:
    · 函数/类定义统计
    · 继承与装饰器使用
    · 推导式、生成器
    · 内置函数调用
    · 嵌套深度追踪
    · 魔法数字检测
    · 测试/验证/异常处理
    · 类型注解覆盖率
    · ...
    │
    ▼
CTCodeAnalyzer._evaluate_*()  [五维度评分器]
  - 每维度5-8个特征子项
  - 归一化 + 加权合成
  - 输出1-5分制得分
    │
    ▼
EvaluationResult  [评估结果]
  - 五维度得分 + 详细特征分解 + 代码证据
```

**关键设计原则**：**每一条评分规则都可追溯到具体的代码行/结构**。这意味着教师可以直接引用评估结果向学生解释得分依据，例如"你的'算法设计'得分偏低，因为检测到4层嵌套循环，建议使用函数提取来展平代码结构"。

**可配置阈值**：系统所有评分阈值都集中在`CONFIG`字典中（如函数粒度阈值、嵌套深度阈值、类型注解覆盖率目标等），教师可根据课程要求自由调整。

**动态测试集成**：除静态分析外，系统支持教师提供单元测试文件（pytest格式），将测试通过率以40%的权重融入"评估"维度得分。这实现了静态代码质量 + 动态功能正确性的综合评估。

### 4.8 三层保真报告生成机制

这是本系统的关键创新。在教育场景中，AI生成的诊断报告若包含错误或"幻觉"（模型凭空编造的数据），将直接误导教学决策。为此，设计了逐层约束的保真机制：

```
          输入：算法原始输出（数据、分类、优先级）
                         │
┌────────────────────────┼────────────────────────┐
│  第一层：程序模板强制填充（100%保真）              │
│  · 每个数字、每个分类、每个维度名称都由代码直接生成  │
│  · 零自由度——大模型尚未参与，保证数据绝对准确       │
│  · 生成的"结构化模板报告"作为后续校验的基准         │
└────────────────────────┼────────────────────────┘
                         │
                         ▼
┌────────────────────────┼────────────────────────┐
│  第二层：大模型语言润色（数据不动，文字动）         │
│  · 大模型收到严格约束指令：                        │
│    - 禁止修改任何数字                              │
│    - 禁止修改分类标签（✅⚠️💡➖）                   │
│    - 禁止调整优先级顺序                            │
│    - 禁止遗漏任何维度                              │
│    - 禁止添加未提及的内容                          │
│  · 允许的操作：优化句子衔接、段落过渡、                      │
│    自然化语言表达、调整标点格式                    │
│  · 润色引擎自动切换：本地Ollama → DeepSeek → 放弃润色  │
└────────────────────────┼────────────────────────┘
                         │
                         ▼
┌────────────────────────┼────────────────────────┐
│  第三层：程序自动保真度校验                        │
│  · 逐条核对润色后的报告是否100%忠实于算法原始数据   │
│  · 5项校验：                                      │
│    1. 所有✅优先干预维度是否都在报告中              │
│    2. 所有5个维度是否一个不少                      │
│    3. 各维度因果效应数值是否一致（允许±0.5舍入）    │
│    4. 分类标签（✅⚠️💡➖）是否被篡改               │
│    5. 学生编号是否正确                             │
│  · 通过 → 使用润色版；失败 → 自动回退到100%保真模板 │
└──────────────────────────────────────────────────┘
```

### 4.9 大模型润色引擎自动切换

系统实现了三级自动降级策略，确保润色服务的鲁棒性（Robustness，即系统在部分组件失效时仍能正常运作）：

```
[1] 尝试本地 Ollama（qwen:7b 模型）
    │
    ├── 成功 → 返回润色结果
    │
    └── 失败/超时/不可用 → 
        │
        │ 打印 "[润色] Ollama 不可用，切换至 DeepSeek API"
        │
        ▼
[2] 尝试 DeepSeek API（deepseek-chat 模型）
    │
    ├── 成功 → 返回润色结果
    │
    └── 失败/密钥未配置/网络不可用 →
        │
        │ 打印 "[润色] DeepSeek API 失败，使用模板报告"
        │
        ▼
[3] 直接使用模板报告（保真度100%，无需大模型）
```

**技术细节**：
- **Ollama**：本地部署的开源大模型推理服务，通过HTTP REST API调用，低延迟、无外网依赖。超时时间90秒
- **DeepSeek API**：云端大模型服务，通过OpenAI兼容接口调用。base_url设为`api.deepseek.com`，模型参数temperature=0.4（低随机性，保证输出稳定），超时30秒
- **失败处理**：所有失败（连接拒绝、超时、异常）统一返回`None`，不抛出异常，不阻塞主流程
- **环境变量管理**：DeepSeek密钥通过环境变量`DEEPSEEK_API_KEY`或Streamlit Secrets管理，不硬编码在源码中

---

## 五、系统工作流程

### 5.1 单次诊断完整流程

```
Step 1: 数据输入
  ├── 通道A: 量表得分CSV/Excel上传 或 手动滑块输入
  └── 通道B: Python代码上传 或 粘贴

Step 2: 模型准备
  ├── 加载参考模型（200条模拟数据预训练的XGBoost）
  ├── 或: 在用户上传数据上重新训练模型
  └── 计算自适应阈值（SHAP阈值、因果效应阈值）

Step 3: Layer 1 — 反事实分析
  ├── 对每个维度模拟1→5分的预测变化
  ├── 输出: 各维度提升潜力排名
  └── 识别"假设最优干预维度"

Step 4: Layer 2 — 双维度诊断
  ├── 计算SHAP值（模型归因重要性）
  ├── 计算因果效应（后门调整ATE）
  ├── 交叉分类 → 四象限干预矩阵
  ├── 对⚠️维度追溯根因（DAG上游分析）
  └── 输出: 四分类干预建议 + 散点图

Step 5: 生成教师诊断报告
  ├── 第一层: 程序模板填充（100%保真）
  ├── 第二层: 大模型语言润色（可选，引擎自动切换）
  ├── 第三层: 程序自动保真度校验（5项核对）
  └── 输出: Markdown格式诊断报告

Step 6: 报告保存与导出
  ├── Markdown文件下载
  ├── 模板报告备份
  └── 批量模式: CSV/Excel汇总导出
```

### 5.2 批量诊断模式

支持对多学生数据（量表CSV/Excel或代码ZIP包）进行批量诊断，依次对每个学生运行完整的诊断管道，生成汇总表（含预测成绩、各维度得分、优先干预维度），并支持个体报告的逐份查看。

---

## 六、技术体系总览

| 层次 | 方法/工具 | 解决的问题 | 输出形式 |
|------|-----------|-----------|----------|
| **数据采集层** | 15题李克特量表 / AST静态分析 | 获取学生五维度计算思维得分 | 结构化数值向量 + 代码行为指标 |
| **预测建模层** | XGBoost（100棵树，深度3） | 建立"五维度→成绩"的非线性映射 | 预测作业质量（0-100分），RMSE误差 |
| **可解释层** | SHAP（Shapley博弈论归因） | 解释模型为何给出此预测（哪些维度贡献大） | 每个维度的SHAP值（正/负贡献） |
| **因果推断层** | 后门调整（OLS + 分组回退） | 区分相关性与因果性，估计干预效果 | 每个维度的因果效应（预期提分/分） |
| **诊断分类层** | 双维度矩阵（SHAP × 因果效应） | 将分析结果转化为教学行动指南 | 四分类干预建议（✅⚠️💡➖） |
| **反事实层** | 单维度CFE模拟 | 展示"如果提升某维度"的预期结果 | 假设变化表 + 最佳干预候选 |
| **ITE估计层** | X-Learner / T-Learner | 估计个体学生对干预的响应 | 个体处理效应 + 干预推荐分级 |
| **报告生成层** | 模板填充 + LLM润色 + 程序校验 | 生成准确且易读的教师诊断报告 | Markdown报告（保真度经验证） |
| **交互展示层** | Streamlit Web应用 | 提供可视化交互式诊断界面 | 双通道输入 + 散点图 + 下载导出 |

---

## 七、技术局限与展望

### 7.1 当前局限

1. **因果图依赖专家知识**：当前DAG结构基于教育理论假设，若因果关系设定有误，后门调整的结果会受"模型设定偏误"影响。后续可通过因果发现算法（如PC算法、FCI算法）从数据中学习因果结构作为交叉验证
2. **样本量约束**：XGBoost在200条模拟数据上训练，模型泛化能力受限于训练数据的代表性。实际部署时需收集真实学生数据持续更新模型
3. **ITE需要实验数据**：Layer 3的个体效应估计需要前测/后测实验数据，这在日常教学中较难获取
4. **AST仅支持Python**：当前代码评估模块只处理Python语法树，不支持其他编程语言（如Scratch、Java、C++等）
5. **润色依赖外部服务**：大模型润色依赖本地Ollama运行或DeepSeek API网络连接，任一服务的波动都可能影响润色功能的可用性（但不影响核心诊断功能，有模板回退）

### 7.2 未来方向

- **多语言AST支持**：扩展到JavaScript、Java等语言，覆盖更广的信息技术课程
- **纵向追踪**：支持同一学生多个时间点的诊断对比，追踪计算思维发展轨迹
- **自动因果发现**：从数据中学习最可能的因果图结构，减少对人工设定DAG的依赖
- **干预方案自动生成**：基于诊断结果自动生成个性化的训练材料和练习题推荐

---

## 八、总结

本系统以"可解释"和"因果性"为核心原则，构建了一套从数据采集到个性化干预建议的完整技术链路。其关键创新点在于：

1. **SHAP与因果推断的融合诊断**：用SHAP值解释模型的"观点"（模型认为什么重要），用因果效应验证真实的"规律"（干预什么真的有效），两者交叉形成可操作的四分类干预建议
2. **三层保真报告机制**：通过程序模板强约束、大模型仅做文字润色、事后自动校验的三重保障，确保AI生成的诊断报告不会"说谎"——这在教育场景中至关重要
3. **双通道设计**：量表评估与代码分析两种输入模式，分别适用于不同的教学场景（理论课vs编程课），共享同一套诊断管道
4. **自适应阈值**：诊断分类的阈值不依赖人工经验设定，而是基于训练数据分布自动计算，增强了系统在不同样本群体间的迁移能力

系统当前以Streamlit Web应用形式部署，支持本地运行和云端部署（Streamlit Cloud），面向中学信息技术教师，提供即时的、有理论支撑的、可解释的计算思维诊断与干预建议。

---

*本报告基于"计算思维诊断系统v6.1"的技术实现撰写。系统源码采用Python开发，核心依赖包括XGBoost、SHAP、statsmodels、Streamlit等开源项目。*
