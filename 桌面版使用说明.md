# 桌面版使用说明

本系统提供两种本地使用方式，均无需联网即可完成诊断（仅「报告润色」等大模型功能需要联网 + 自己的 DeepSeek API Key）。

---

## 方式一：桌面版 exe（推荐给一线教师）

### 打包产物

运行 `python -m PyInstaller CT_diagnostic.spec` 后，在 `dist/CT_Diagnostic/` 目录下得到：

```
CT_Diagnostic/
├── CT_Diagnostic.exe      ← 双击这个启动
└── _internal/             ← 依赖与数据文件（勿删）
```

### 使用方法

1. 把整个 `CT_Diagnostic` 文件夹复制给教师（约 720 MB，可用 U 盘或网盘）。
2. 教师双击 `CT_Diagnostic.exe`。
3. 等待约 10–30 秒，浏览器会自动打开 `http://localhost:8000`。
4. 在浏览器里上传量表/代码 → 诊断 → 生成干预题单。
   - 干预题单内置 150 道母题（五维各 30 道，难度 1–3 星），**跨周自动去重**：发过的题下周不会重复出现；题库不足时会明确提示，可点「清空出题历史」重新开始一轮。
   - 「一人一单」：批量诊断后，系统按每名学生的薄弱维度自动生成个人题单＋分发清单（照单发放），上传表格可加「姓名」「班级」列。

### 数据存放位置

- 生成的诊断题单、导出文件，保存在 `CT_Diagnostic` 文件夹旁的 `data/` 目录。
- 量表、诊断结果由教师自行保存下载，不会上传到任何服务器。

### 重新打包

修改代码后，重新执行：

```bash
pip install pyinstaller
pyinstaller CT_diagnostic.spec --noconfirm
```

---

## 方式二：一键启动脚本（需要先装 Python）

如果教师电脑不方便拷贝 720 MB 的 exe，也可以用 `启动系统.bat`：

1. 教师电脑先装 Python 3.10+（安装时勾选 **Add Python to PATH**）。
2. 把整个项目文件夹复制过去，双击 `启动系统.bat`。
3. 首次运行会自动安装依赖（约 3–5 分钟），之后每次双击即可启动。

---

## 关于 DeepSeek API Key

- 报告润色功能需要 DeepSeek API Key。
- **每位教师应在网页里输入自己的 key**（不会上传、只存在当前浏览器会话），避免消耗他人额度。
- 不输入 key 时：润色回退模板报告，其余功能不受影响。出题不再依赖大模型，直接用内置 150 道题库并跨周去重。
