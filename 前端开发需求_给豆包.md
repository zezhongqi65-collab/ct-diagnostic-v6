# 计算思维诊断系统 — 前端开发需求（对接文档）

> 给前端开发者（豆包）的接口规范与需求清单。后端已用 Python 完成，前端**只做 UI 和渲染，不要实现任何诊断算法**。

---

## 一、背景与分工

- **后端（已完成）**：Python FastAPI，提供真实诊断引擎（XGBoost 预测 + TreeSHAP 归因 + 后门调整因果推断）。文件 `backend.py`。
- **前端（你负责）**：单文件 `static/index.html`，调用后端 API，渲染结果。
- **⚠️ 最重要的一条**：前端**不要写死任何诊断公式、权重、阈值**。作业质量预测、SHAP、因果效应、四分类——全部从 API 返回的 JSON 里取。之前那个「作业质量 = 8·抽象 + 4·分解 + …」的写死公式是错的，那是模拟数据的生成公式，不能用于真实诊断。

---

## 二、后端启动方式

```bash
cd 项目目录
python -m uvicorn backend:app --host 127.0.0.1 --port 8000
```

前端放在 `static/index.html`，通过 `http://localhost:8000/` 访问。

---

## 三、API 规范

### 3.1 POST `/api/diagnose` — 单个学生诊断

**请求体**（JSON）：

```json
{
  "student_id": "S001",
  "scores": {"抽象": 2.0, "分解": 3.5, "算法设计": 4.0, "建模": 3.0, "评估": 2.5}
}
```

`scores` 的 key 固定是这 5 个中文维度名：`抽象`、`分解`、`算法设计`、`建模`、`评估`，值范围 1–5。

**响应体**（JSON）：

```json
{
  "student_id": "S001",
  "base_pred": 73.8,
  "scores": {"抽象": 2.0, "分解": 3.5, "算法设计": 4.0, "建模": 3.0, "评估": 2.5},
  "causal_order": ["抽象", "分解", "算法设计", "建模", "评估"],
  "dim_names": {
    "抽象": "模式识别与抽象",
    "分解": "问题分解",
    "算法设计": "算法与步骤设计",
    "建模": "数学建模与符号化",
    "评估": "方案评估与调试"
  },
  "cf_best_dim": "抽象",
  "cf_best_gain": 8.2,
  "shap_threshold": 2.3,
  "ce_threshold": 4.1,
  "layer1": [
    {
      "dim": "抽象",
      "current": 2.0,
      "series": [
        {"抽象假设值": 1, "预测成绩": 70.1, "相比当前变化": -3.7},
        {"抽象假设值": 5, "预测成绩": 82.0, "相比当前变化": 8.2}
      ]
    }
  ],
  "layer2": [
    {
      "维度": "抽象",
      "得分": 2.0,
      "SHAP": 0.52,
      "因果效应": 8.2,
      "分类": "优先干预",
      "建议": "立即安排模式识别与抽象专项训练（预计提升8.2分）",
      "优先级": 0
    }
  ]
}
```

**字段说明（前端渲染必读）**：

| 字段 | 含义 | 用法 |
|------|------|------|
| `base_pred` | 预测作业质量（0–100） | 大数字 hero |
| `cf_best_dim` / `cf_best_gain` | 反事实最优维度与预计提分 | 「优先提升 X 预计 +Y 分」 |
| `shap_threshold` / `ce_threshold` | 自适应阈值 | 散点图两条虚线 |
| `layer1[].series[].{维度}假设值` | ⚠️ 字段名是**动态的**，如「抽象假设值」，等于该维度名 + 「假设值」 | 反事实表 X 轴 |
| `layer2[].分类` | 四分类之一 | 颜色 / pill 标签 |

**四分类取值（`layer2[].分类` 只有这 4 种）**：`优先干预`、`仅观察`、`潜在有效`、`无需关注`。

**`layer2[].优先级`**：0=优先干预，1=仅观察，2=潜在有效，3=无需关注。

### 3.2 POST `/api/batch` — 批量诊断

**请求体**：

```json
{
  "students": [
    {"student_id": "S001", "scores": {"抽象": 2.0, "分解": 3.5, "算法设计": 4.0, "建模": 3.0, "评估": 2.5}},
    {"student_id": "S002", "scores": {"抽象": 4.0, "分解": 2.0, "算法设计": 3.0, "建模": 4.5, "评估": 3.0}}
  ]
}
```

**响应体**：

```json
{ "results": [ {…同 3.1 的单个诊断结果…}, {…} ] }
```

批量分组逻辑：对每个结果，取其 `layer2` 里**第一个 `分类 === "优先干预"` 的维度**作为分组键；没有优先干预的归入「综合」组。（这个分组可以前端做，也可以后端加，前端做即可。）

---

## 四、功能需求清单

### 视图 1：单人诊断

1. **两种输入模式**（切换）：
   - 模式 A：五维 slider（直接拖 1–5，0.1 步长）
   - 模式 B：15 题量表输入（输入 A1–A15 每题 1–5 分，前端按下方规则聚合成五维，再调 API）
2. 点「开始诊断」→ 调 `/api/diagnose` → 渲染结果。

**渲染内容**（都要有）：
- 预测作业质量大数字
- 五维雷达图（`scores` + `dim_names`）
- 四分类卡片列表（`layer2`，每维：分类 pill + SHAP + 因果效应 + 建议文字）
- 四象限散点图：X = `|SHAP|`，Y = `因果效应`，颜色按 `分类`，画 `shap_threshold` 竖虚线 + `ce_threshold` 横虚线
- 反事实表（`layer1`）：每维当前分 → 提至满分的预测变化
- 教师诊断报告（文字段落，数据全部来自 API，按「优先干预 / 仅观察 / 潜在有效 / 无需关注」分组叙述）

### 视图 2：批量诊断

- 粘贴多行数据（每行：`学生ID,抽象,分解,算法设计,建模,评估`，首行表头）
- 调 `/api/batch` → 汇总表（学生 + 预测分 + 各维得分 + 优先干预维度）+ 干预分组 + 导出 CSV

### 视图 3：方法说明

- 五个维度与因果 DAG（抽象→分解、抽象→算法设计、算法设计→建模、算法设计→评估）
- 四象限分类规则表
- 个体化调制说明（消除「满分维度仍建议干预」的生态谬误）

---

## 五、15 题量表映射规则（前端实现）

| 维度 | 题号 | 计算 |
|------|------|------|
| 分解 | A1, A2, A3 | 三题平均 |
| 抽象 | A4, A5, A6 | 三题平均 |
| 建模 | A7, A8 | 两题平均 |
| 算法设计 | A9, A10 | 两题平均 |
| 评估 | A11–A15 | 五题平均 |

每题 1–5 分（A=完全不符合=1 … E=完全符合=5）。算出的五维平均分作为 `scores` 传给 API。

---

## 六、设计风格（你擅长，简单提）

- 学术纸面风格：米色纸面背景、深绿侧边栏、衬线字体标题（Noto Serif SC）
- 四分类配色：优先干预=绿 #2F9E6E、仅观察=橙 #D08A12、潜在有效=蓝 #2F7FB8、无需关注=灰 #98A5B0
- 图表用 ECharts（雷达图 + 散点图）

---

## 七、对接自测方法

1. 启动后端后，浏览器控制台跑一句：
   ```js
   fetch('/api/diagnose',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({student_id:'T1',scores:{抽象:2,分解:3,算法设计:4,建模:3,评估:2.5}})}).then(r=>r.json()).then(console.log)
   ```
2. 确认返回 JSON 含 `base_pred`、`layer1`、`layer2` 即对接成功。

---

## 八、硬性约束（务必遵守）

1. **不要在前端写死任何诊断公式、权重、阈值** —— 全部从 API 取。
2. **`layer1[].series` 的字段名是动态的**（`{维度}假设值`），解析时用 `series[0][dim + "假设值"]` 这种动态取值，别写死 `假设值`。
3. 四分类文案、emoji 严格用 API 返回的 `分类` 字段，不要自己造。
